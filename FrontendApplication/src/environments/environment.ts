// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyD1spvB5NR-W83avDtv-WFLYiobpGbAb_A',
    authDomain: 'well-scrubbler.firebaseapp.com',
    databaseURL: 'https://well-scrubbler.firebaseio.com',
    projectId: 'well-scrubbler',
    storageBucket: 'well-scrubbler.appspot.com',
    messagingSenderId: '526493635644',
    appId: '1:526493635644:web:0feed512d37dd8febc5f39'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
