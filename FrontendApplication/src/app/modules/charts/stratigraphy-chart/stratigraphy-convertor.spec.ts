import { StratigraphyConvertor } from './stratigraphy-convertor';

describe('StratigraphyConvertor', () => {
  it('should create an instance', () => {
    expect(new StratigraphyConvertor()).toBeTruthy();
  });
});
